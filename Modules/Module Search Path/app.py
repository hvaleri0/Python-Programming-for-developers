import sales  # when python sees this it will look for a file "sales.py" in current directory
import sys

# provides a list off all the directories it will search until it finds the module.
print(sys.path)
