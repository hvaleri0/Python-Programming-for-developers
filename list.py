letters = ["a", "b", "c"]
matrix = [[1, 2], [3, 4]]

zeros = [0] * 5  # use asterik to clone 5 times

combine = zeros + letters  # use plus to concatanate

# use list & range function to iterate numbers sequentially
numbers = list(range(20))

chars = list("Hello World")  # use list to iterate over string as well
print(chars)
